# Challenge 05

The stack `ctfwksp/challenge-05` is failing with:

`error: CTF deployment cannot use dev database: dev-db.example.com`

Find the configuration issue and fix it.

**Success Criteria:** Successful Deployment
