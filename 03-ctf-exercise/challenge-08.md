# Challenge 08

The environment `ctfwksp/challenge-08` is configured to surface a secret from an Azure Key Vault, but it's incomplete. Azure credentials are available in `workshop/azure-credentials`. The Vault name is ctf-vaultc0727b1e and the key is api-key.

Update `ctfwksp/challenge-08` so the secret value is accessible when the environment is opened.

**Success Criteria:** Successfully open `ctfwksp/challenge-08` and share the secret value
