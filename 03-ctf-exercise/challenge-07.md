# Challenge 07

The stack `ctfwksp/challenge-07` is failing with:

`error: Unexpected app name: workshop-app`

The stack expects `name` to be `my-custom-app`. Find the configuration issue and fix it.

**Success Criteria:** Successful Deployment
