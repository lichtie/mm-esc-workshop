# Challenge 01

The stack in `./01-stack` processes config values from `ctfwksp/challenge-01`, which in turn imports `ctfwksp/01-base-layer`.

The stack is failing with the error `error: Configuration 'ctfwksp:appPort' value '[8443]' is not a valid number`. Find what's wrong with the configuration and fix it.

Success Criteria: Successful Deployment
